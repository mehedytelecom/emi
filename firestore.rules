rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read & write access for Mehedi Telecom EMI Management
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

