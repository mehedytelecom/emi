rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow full read and write for any authenticated session (Google or Admin PIN Auth)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
